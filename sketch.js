function setup() {
  createCanvas(500, 400);
    background("orange")
}

function draw() {

  fill("red")
rect(mouseX,mouseY,100,150,50);//retagungulo(pos x,pos y,tam x,tam y)
 stroke("blue"); 
  (mouseIspressed)  background("red")
}
